<!DOCTYPE html>
<html lang="en">
<head>
  <title>Jolly Up Website</title>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link href="/achievements.css" rel="stylesheet" type="text/css">
  <link rel ="stylesheet" href="bower_components/bootstrap/dist/css/bootstrap.min.css">
  <script src="bower_components/bootstrap/dist/js/bootstrap.min.js"></script>
</head>

<body>
  <nav class="navbar navbar-trans">
      <div class="container-fluid">
        <div class="navbar-header">
            <a class="navbar-brand" href="#">Jawbone Data Website</a>
        </div>
        <div>
            <ul class="nav navbar-nav">
              <li><a href="/dashboard?user=<%= userID %>">Dashboard</a></li>
              <li><a href="/levels?user=<%= userID %>">Levels</a></li>
              <li class="active"><a href="/achievements?user=<%= userID %>">Achievements</a></li>
              <li><a href="/teamPage?user=<%= userID %>">Team</a></li>
              <li><a href="/weeklyChallenges?user=<%= userID %>">Weekly Challenges</a></li>
            </ul>
        </div>
      </div>
  </nav>

  <div class="col-md-4">
    <table class="achievementsTable table table-bordered">
      <thead>
        <tr>
          <th>Achievement</th>
          <th>Description</th>
        </tr>
      </thead>
      <tbody>
        <% for(var i=0; i < normalArray.length; i++) { %>
        <tr>
          <td> <%= normalArray[i] %> </td>
          <td><%= normalArrayData[i] %></td>
        </tr>
        <% } %>
      </tbody>
    </table>
  </div>
  <div class = "col-md-4">
    <table class="achievementsTable table table-bordered">
      <thead>
        <tr>
          <th>Achievement</th>
          <th>Description</th>
        </tr>
      </thead>
      <tbody>
        <% for(var i=0; i < consecArray.length; i++) { %>
        <tr>
          <td> <%= consecArray[i] %> </td>
          <td><%= consecArrayData[i] %></td>
        </tr>
        <% } %>
      </tbody>
    </table>
  </div>
  <div class = "col-md-4">
    <table class="achievementsTable table table-bordered">
      <thead>
        <tr>
          <th>Achievement</th>
          <th>Description</th>
        </tr>
      </thead>
      <tbody>
        <% for(var i=0; i < totalArray.length; i++) { %>
        <tr>
          <td> <%= totalArray[i] %> </td>
          <td><%= totalArrayData[i] %></td>
        </tr>
        <% } %>
      </tbody>
    </table>
  </div>

  </div>
</body>
